* Write script to cross reference applications with letters of reference. √
* Write script to output pdfs for applications and letters of reference individually. √
    For simplicity, this should only run once all applicants have exactly one matching letter of reference.

* Write script to assign applications and create CSV files for ratings for each reader.
    Input: List of names *not* to assign to each reader AND list of readers. Also applicant names.
    Output: Saved CSV files with random list of applicants and column for score and notes.
            Combined applications based on output of above CSV file.
            Each in own reader's folder.

* Write script to combine CSV rating files into a master spreadsheet (OR do in google sheets!).


BONUS: Write script to print stats about applications. Female, Male, Year, etc.
